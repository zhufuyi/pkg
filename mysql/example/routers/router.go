package routers

/*
// NewRouter 实例化路由
func NewRouter() *gin.Engine {
	r := gin.New()
	r.Use(gin.Recovery())
	r.Use(middleware.Logger())
	binding.Validator = validator.Init()

	if global.Conf.RunMode == "prod" {
	} else {
	}

	apiv1 := r.Group("/api/v1")
	{
		apiv1.POST("/userExample", v1.CreateUserExample)
		apiv1.DELETE("/userExample", v1.DeleteUserExample)
		apiv1.DELETE("/userExamples", v1.DeleteUserExamples)
		apiv1.PUT("/userExample", v1.UpdateUserExample)
		apiv1.GET("/userExample/:id", v1.GetUserExample)
		apiv1.GET("/userExamples", v1.GetUserExamples)
	}

	return r
}
*/
